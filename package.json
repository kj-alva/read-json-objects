{
  "name": "read-json-objects",
  "version": "1.0.0",
  "description": "Read json objects and return specific elements",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "karthik.alva",
  "license": "ISC",
  "dependencies": {
    "prompt": "^1.1.0"
  }
}
